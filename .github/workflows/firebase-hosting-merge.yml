name: Deploy to Firebase Hosting
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          test -n "$VITE_ADMIN_ACCESS_KEY" || { echo "Missing VITE_ADMIN_ACCESS_KEY secret"; exit 1; }
          npm ci --legacy-peer-deps
          npm run build
        env:
          VITE_FIREBASE_API_KEY: '${{ secrets.VITE_FIREBASE_API_KEY }}'
          VITE_FIREBASE_AUTH_DOMAIN: '${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}'
          VITE_FIREBASE_PROJECT_ID: '${{ secrets.VITE_FIREBASE_PROJECT_ID }}'
          VITE_FIREBASE_STORAGE_BUCKET: '${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}'
          VITE_FIREBASE_MESSAGING_SENDER_ID: '${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}'
          VITE_FIREBASE_APP_ID: '${{ secrets.VITE_FIREBASE_APP_ID }}'
          VITE_FIREBASE_MEASUREMENT_ID: '${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}'
          VITE_GEMINI_API_KEY: '${{ secrets.VITE_GEMINI_API_KEY }}'
          VITE_ADMIN_ACCESS_KEY: '${{ secrets.VITE_ADMIN_ACCESS_KEY }}'
          VITE_ENABLE_CLIENT_SEED: '${{ secrets.VITE_ENABLE_CLIENT_SEED }}'
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PHISHGUARD }}'
          channelId: live
          projectId: phish-guard-edu
          target: phish-guard-dev